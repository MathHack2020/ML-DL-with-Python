streamlit>=1.25.0
tensorflow>=2.15.0  # For deep learning model with Keras (ensure compatibility with .keras format)
xgboost>=1.7.0      # For the XGBoost model
numpy>=1.21.0       # For numerical operations
pandas>=1.5.0       # For data manipulation (if using DataFrames)
scikit-learn>=1.0.0  # For preprocessing (e.g., StandardScaler, train_test_split)
joblib>=1.1.0       # For saving/loading the scaler (optional, for persistence)